@{
    ViewData["Title"] = "API";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
<link rel="preconnect" href="https://rsms.me/">
<link rel="stylesheet" href="https://rsms.me/inter/inter.css">

<style>
    body {
        background: linear-gradient(135deg, #0f0f0f 0%, #1a1a1a 100%);
        color: #fff;
        font-family: 'Inter', sans-serif;
    }

    /* Hero Section */
    .hero {
        text-align: center;
        padding: 6rem 2rem 4rem;
        background: url('https://earthobservatory.nasa.gov/ContentFeature/BlueMarble/Images/land_shallow_topo_2048.jpg') no-repeat center center / cover;
        position: relative;
    }

        .hero::after {
            content: "";
            position: absolute;
            inset: 0;
            background: rgba(0,0,0,0.7);
        }

    .hero-inner {
        position: relative;
        z-index: 1;
        max-width: 800px;
        margin: auto;
    }

    .hero h1 {
        font-size: 3rem;
        font-weight: 800;
        background: linear-gradient(90deg, #10b981, #34d399);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .hero p {
        color: #ccc;
        font-size: 1.2rem;
        margin-top: 1rem;
        font-style: italic;
    }

    /* Section headings */
    h2 {
        font-weight: 700;
        margin-top: 2.5rem;
        margin-bottom: 1rem;
        background: linear-gradient(90deg, #10b981, #34d399);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    /* Endpoint cards */
    .endpoint-card {
        background: rgba(25,25,25,0.85);
        border: 1px solid rgba(255,255,255,0.08);
        border-radius: 1rem;
        padding: 1rem 1.5rem;
        margin-bottom: 1rem;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

        .endpoint-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(16,185,129,0.25);
        }

        .endpoint-card strong {
            color: #10b981;
        }

    /* Code blocks */
    pre {
        background: #111;
        color: #10b981;
        padding: 1rem;
        border-radius: 0.75rem;
        overflow-x: auto;
        font-size: 0.9rem;
    }

    /* Notes list */
    ul {
        color: #aaa;
    }

    /* CTA */
    .cta {
        text-align: center;
        padding: 3rem 2rem;
        background: rgba(16, 185, 129, 0.1);
        border-top: 1px solid rgba(255,255,255,0.1);
        margin-top: 3rem;
    }

        .cta h2 {
            margin-bottom: 1rem;
        }

        .cta p {
            color: #aaa;
            margin-bottom: 1.5rem;
        }

        .cta .btn-primary {
            background: #10b981;
            border: none;
            font-weight: 600;
            border-radius: 0.75rem;
            padding: 0.75rem 1.5rem;
            transition: all 0.3s ease;
        }

            .cta .btn-primary:hover {
                box-shadow: 0 0 18px rgba(16, 185, 129, 0.6);
            }
</style>

<!-- Hero -->
<section class="hero">
    <div class="hero-inner">
        <h1>Hero API</h1>
        <p>Coming Soon... Automate your sustainability reporting</p>
    </div>
</section>

<!-- Overview -->
<div class="container py-5">
    <h2>Overview</h2>
    <p>
        The Hero API allows corporate users to submit and retrieve carbon emissions data programmatically.
        This includes electricity usage, fuel consumption, transport distance, and waste generation.
        The API is designed to provide companies with accurate and automated reporting for ESG compliance.
    </p>

    <!-- Endpoints -->
    <h2>Endpoints</h2>
    <div class="endpoint-card"><strong>POST /api/corporate/emissions</strong> – Submit monthly emissions data.</div>
    <div class="endpoint-card"><strong>GET /api/corporate/emissions/{companyId}</strong> – Retrieve emission reports for a company.</div>
    <div class="endpoint-card"><strong>POST /api/corporate/subscription</strong> – Create a subscription (not active yet).</div>
    <div class="endpoint-card"><strong>GET /api/corporate/subscription/{companyId}</strong> – Check subscription status (not active yet).</div>

    <!-- Request/Response -->
    <h2>Request/Response Example</h2>
    <p><strong>POST /emissions</strong> request body:</p>
    <pre>
{
    "CorporateCompanyId": 1,
    "Year": 2025,
    "Month": 12,
    "ElectricityKWh": 500,
    "FuelLiters": 200,
    "TransportKm": 300,
    "WasteKg": 50
}
    </pre>

    <p><strong>Response:</strong></p>
    <pre>
{
    "Id": 1,
    "CorporateCompanyId": 1,
    "Year": 2025,
    "Month": 12,
    "ElectricityEmissions": 150,
    "FuelEmissions": 400,
    "TransportEmissions": 90,
    "WasteEmissions": 20,
    "TotalEmissions": 660
}
    </pre>

    <!-- Notes -->
    <h2>Notes</h2>
    <ul>
        <li>The API service is currently inactive (Coming Soon).</li>
        <li>Once live, users will receive an API key to authenticate requests.</li>
        <li>Full documentation with all DTOs and usage examples will be available when the service is launched.</li>
    </ul>
</div>

<!-- CTA -->
<section class="cta">
    <h2>Stay Tuned</h2>
    <p>Our API is under development and will be available shortly.</p>
    <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-primary">Get Notified</a>
</section>
