
@{
    ViewData["Title"] = "About";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
<link rel="preconnect" href="https://rsms.me/">
<link rel="stylesheet" href="https://rsms.me/inter/inter.css">

<style>
    body {
        background: linear-gradient(135deg, #0f0f0f 0%, #1a1a1a 100%);
        color: #fff;
        font-family: 'Inter', sans-serif;
        overflow-x: hidden;
    }

    /* Hero Section */
    .hero {
        position: relative;
        text-align: center;
        padding: 6rem 2rem 4rem;
        background: url('/images/earth-space.jpg') no-repeat center center / cover;
    }

        .hero::after {
            content: "";
            position: absolute;
            inset: 0;
            background: rgba(0,0,0,0.6);
        }

    .hero-inner {
        position: relative;
        z-index: 1;
        max-width: 800px;
        margin: 0 auto;
    }

    .hero h1 {
        font-size: 2.8rem;
        font-weight: 800;
        background: linear-gradient(90deg, #10b981, #34d399);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .hero p {
        color: #ccc;
        font-size: 1.1rem;
        margin-top: 1rem;
    }

    /* About Content */
    .about-content {
        padding: 4rem 2rem;
        max-width: 900px;
        margin: 0 auto;
        text-align: center;
    }

        .about-content h2 {
            font-weight: 700;
            margin-bottom: 1rem;
            background: linear-gradient(90deg, #10b981, #34d399);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .about-content p {
            color: #aaa;
            line-height: 1.7;
            margin-bottom: 2rem;
        }

    /* Earth Image */
    .earth-image img {
        max-width: 600px;
        border-radius: 1rem;
        box-shadow: 0 0 25px rgba(16,185,129,0.5);
        transition: transform 0.5s ease;
    }

        .earth-image img:hover {
            transform: scale(1.05);
        }

    /* Core Values */
    .values {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 2rem;
        margin-top: 2rem;
        text-align: left;
    }

    .value-card {
        background: rgba(25,25,25,0.85);
        border: 1px solid rgba(255,255,255,0.08);
        border-radius: 1rem;
        padding: 1.5rem;
        backdrop-filter: blur(10px);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

        .value-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(16,185,129,0.25);
        }

        .value-card h4 {
            color: #10b981;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .value-card p {
            color: #aaa;
            font-size: 0.95rem;
            margin: 0;
        }

    /* CTA Section */
    .cta {
        text-align: center;
        padding: 4rem 2rem;
        background: rgba(16, 185, 129, 0.1);
        border-top: 1px solid rgba(255,255,255,0.1);
    }

        .cta h2 {
            font-weight: 700;
            margin-bottom: 1rem;
        }

        .cta p {
            color: #aaa;
            margin-bottom: 2rem;
        }

        .cta .btn-primary {
            background: #10b981;
            border: none;
            color: #fff;
            font-weight: 600;
            border-radius: 0.75rem;
            padding: 0.75rem 1.5rem;
            transition: all 0.3s ease;
        }

            .cta .btn-primary:hover {
                box-shadow: 0 0 18px rgba(16, 185, 129, 0.6);
            }

    /* Responsive tweaks */
   @@media (max-width: 576px) {
        .hero h1

    {
        font-size: 2.2rem;
    }

    .hero p {
        font-size: 1rem;
    }

    }</style>

<!-- Hero -->
<section class="hero" aria-label="About Hero">
    <div class="hero-inner">
        <h1>About Us</h1>
        <p>Discover the vision, mission, and values behind Hero.</p>
    </div>
</section>

<!-- About Content -->
<section class="about-content" aria-label="About Content">
    <h2>Our Mission</h2>
    <p>
        We believe meaningful change begins with awareness. Every decision we make how we travel, how we use energy,
        what we consume leaves a mark on the planet. Hero helps you see that mark clearly, measure it, and turn it into
        positive action. Our mission is simple: to empower Egyptian corporations to take responsibility for their carbon
        footprint and to join a global movement toward sustainability.
    </p>

    @* <div class="earth-image mt-4">
        <img src="https://pixelz.cc/wp-content/uploads/2018/07/planet-earth-from-space-uhd-4k-wallpaper.jpg"
             alt="Planet Earth from space" class="img-fluid">
    </div> *@

    <h2 class="mt-5">Our Core Values</h2>
    <div class="values">
        <div class="value-card">
            <h4>Sustainability</h4>
            <p>Every action matters, no matter how small.</p>
        </div>
        <div class="value-card">
            <h4>Awareness</h4>
            <p>Real change begins with understanding.</p>
        </div>
        <div class="value-card">
            <h4>Community</h4>
            <p>Together, we can make the planet greener.</p>
        </div>
    </div>

    <h2 class="mt-5">Personal Effort</h2>
    <p>
        Hero is the result of an individual effort led by <strong>Saef Mohamed</strong>. Every detail from design to backend
        architecture has been crafted with care and dedication. This project reflects a singular vision to deliver a premium,
        credible SaaS experience built from the ground up.
    </p>
</section>

<!-- CTA -->
<section class="cta" aria-label="Call to action">
    <h2>Join Our Journey</h2>
    <p>Be part of a global movement toward sustainability and innovation.</p>
    <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-primary">Create Account</a>
</section>
